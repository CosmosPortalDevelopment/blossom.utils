export interface MessageComponentActions {
    /**
     * Whether to build the action row with the components created
     * 
     * @default true
     * 
     * @note To return the components without an action row, set this to false.
     */
    build_action_row?: boolean;
};