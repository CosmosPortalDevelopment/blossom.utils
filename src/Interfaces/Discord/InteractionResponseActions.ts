export interface InteractionResponseActions {
    /**
     * Whether to build the modal response with the text input components created
     * 
     * @default true
     * 
     * @note To return the text input components without a modal response, set this to false.
     */
    build_modal?: boolean;
};